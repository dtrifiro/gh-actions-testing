name: summary

on:
  push:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        pyv: [
            # "3.9",
            # "3.10",
            "3.13",
          ]
        os: [
            # "macos-latest",
            "ubuntu-latest",
            # "windows-latest"
          ]

    steps:
      - uses: actions/checkout@v4
        with:
          repository: deepseek-ai/DeepGEMM
