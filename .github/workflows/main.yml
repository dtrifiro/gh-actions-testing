name: github actions testing

on:
  push:
  workflow_dispatch:

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
      - name: print token
        run: |
          echo "token is '$GITHUB_TOKEN'"
