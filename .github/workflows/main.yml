name: github actions testing

on:
  push:
  workflow_dispatch:

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
      - name: warn
        run: |
          echo ::warning title="MacOS Tests failed"::"(flaky) tests failed on MacOS"
