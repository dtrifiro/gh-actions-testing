name: Test dynamic uses

on:
    push:

jobs:
    build:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                var: ["", "var_value"]
        steps:
            - uses: actions/checkout@v4
            - run: |
                  echo "$GITHUB_REPO"
                  env | grep GITHUB_
