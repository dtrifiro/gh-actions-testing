name: gh run id

on:
  push:
    tags:
      - "v*\\+test*"
    branches:
      - main
  workflow_dispatch:

jobs:
  testing:
    name: Testing notices
    runs-on: ubuntu-latest
    steps:
      - name: "print notice"
        run: |
          echo "::notice::This is a notice"
