name: Test empty inputs

on:
  push:

  workflow_dispatch:
    inputs:
      test:
        description: "test"
        required: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "target=${{inputs.test}}"
